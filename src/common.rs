#[derive(Debug, Clone)]
pub enum Token<'src> {
    LPar,
    RPar,
    LSqr,
    RSqr,
    Eq,
    Dif,
    And,
    Or,
    Plus,
    Minus,
    Mod,
    Mul,
    Div,
    True,
    False,
    If,
    Then,
    Else,
    Let,
    In,
    Rec,
    Fun,
    Arrow,
    Dot,
    Comma,

    Colon,
    Semi,
    Cons,

    Underscore,
    IntLit(u64),
    FloatLit(f64),
    StringLit(&'src str),
    Iden(&'src str),
    CharLit(char),
}
